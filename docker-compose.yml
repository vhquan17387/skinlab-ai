services:
  postgres:
    image: postgres:16
    container_name: skinai-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: skinai
      POSTGRES_PASSWORD: skinai
      POSTGRES_DB: skinai
    ports:
      - "5432:5432"
    volumes:
      - ./.data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U skinai -d skinai"]
      interval: 5s
      timeout: 5s
      retries: 5
